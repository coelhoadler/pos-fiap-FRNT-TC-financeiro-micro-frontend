customHeaders:
  - pattern: "**"
    headers:
      - key: Access-Control-Allow-Origin
        value: "*"
      - key: Content-Security-Policy
        value: connect-src 'self' http://localhost:3000 localhost:* ws://localhost:* 'unsafe-eval'